projection;
strict ( 2 );
use draft;

define behavior for Z2UI5_R_SMPE_TCK_C alias Ticket
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}